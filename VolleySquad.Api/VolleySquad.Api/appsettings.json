{
  "ConnectionStrings": {
    "DefaultConnection": "Server=DESKTOP-EAS98CR\\LONGG;Database=VolleySquadDB;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True;"
  },
  "JwtSettings": {
    // ⚠️ SECURITY WARNING: Không commit secret key thật lên Git!
    // File này chỉ chứa placeholder cho local dev.
    //
    // SETUP CHO DEVELOPER MỚI:
    //   cd VolleySquad.Api
    //   dotnet user-secrets init
    //   dotnet user-secrets set "JwtSettings:SecretKey" "your-random-256bit-key-here"
    //
    // User Secrets lưu tại: %APPDATA%\Microsoft\UserSecrets\<guid>\secrets.json
    // KHÔNG nằm trong project folder => không bị commit lên Git.
    //
    // PRODUCTION: Dùng Azure Key Vault, AWS Secrets Manager, hoặc
    //   Environment Variable: JwtSettings__SecretKey=xxx (__ = : trong .NET config)
    //
    // ⚠️ Key phải đủ dài: HS256 yêu cầu tối thiểu 256 bit = 32 bytes.
    //   Dùng: openssl rand -base64 32  (để generate key ngẫu nhiên an toàn)
    "SecretKey": "REPLACE-WITH-DOTNET-USER-SECRETS-IN-LOCAL-DEV"
  },
  "RabbitMQ": {
    // ⚠️ SECURITY WARNING: Không dùng guest/guest trên production!
    // RabbitMQ mặc định chặn remote login bằng guest từ phiên bản 3.3+.
    // Chỉ guest@localhost được phép.
    // DEV LOCAL: guest/guest OK (chỉ listen trên localhost).
    // PRODUCTION: Tạo user riêng với vhost riêng, set qua User Secrets hoặc env var.
    "Host": "localhost",
    "Username": "guest",
    "Password": "guest"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // ⚠️ SECURITY: AllowedHosts: "*" cho phép request từ bất kỳ domain nào.
  // PRODUCTION: Đổi thành domain thật của bạn, ví dụ: "volleysquad.com;www.volleysquad.com"
  "AllowedHosts": "*"
}
